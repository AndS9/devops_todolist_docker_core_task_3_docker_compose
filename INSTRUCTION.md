## Running application:
Run this command in directory where docker-compose.yml is:
````
docker-compose up -d
````
## Stopping application:
to stop:
````
docker-compose stop
````
to start:
````
docker-compose start
````

to stop and remove app from processes:
````
docker-compose down
````